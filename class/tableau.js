function Tableau(num, title, text, choices, image, item) {
	var self = this;
	self.num = num;
	self.title = title;
	self.text = text;
	self.choices = choices;
	self.image = image;
	self.item = item;
}